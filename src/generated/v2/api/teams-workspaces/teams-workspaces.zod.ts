/**
 * Generated by orval v7.9.0 🍺
 * Do not edit manually.
 * Meeting BaaS API
 * API for managing meeting bots, calendar integrations, and webhooks
 * OpenAPI spec version: 2.0.0
 */
import { z as zod } from "zod"

/**
 * @summary Create a teams workspace
 */
export const createTeamsWorkspaceBodyNameDefault = "Microsoft Teams"
export const createTeamsWorkspaceBodyNameMax = 100
export const createTeamsWorkspaceBodyDomainMax = 253

export const createTeamsWorkspaceBodyDomainRegExp = /^[a-z0-9.-]+\.[a-z]{2,}$/

export const createTeamsWorkspaceBody = zod.object({
  name: zod
    .string()
    .min(1)
    .max(createTeamsWorkspaceBodyNameMax)
    .default(createTeamsWorkspaceBodyNameDefault)
    .describe(
      "User-friendly label for the workspace. Not unique — names can repeat within a team."
    ),
  domain: zod
    .string()
    .min(1)
    .max(createTeamsWorkspaceBodyDomainMax)
    .regex(createTeamsWorkspaceBodyDomainRegExp)
    .describe(
      "The Microsoft 365 tenant primary domain (e.g. contoso.onmicrosoft.com or a verified custom domain). Identifies the tenant this workspace represents; accounts on any of the tenant's verified domains can be added."
    ),
  extra: zod
    .record(zod.string(), zod.any())
    .describe(
      "An optional free-form JSON object you can attach to the workspace.\n\nStored as-is and never interpreted by the API. Filterable on the list endpoint via the `extra` query parameter using the `key:value` syntax."
    )
    .or(zod.null())
    .optional()
})

/**
 * @summary List teams workspaces
 */
export const listTeamsWorkspacesQueryExtraDefault = null

export const listTeamsWorkspacesQueryParams = zod.object({
  extra: zod
    .string()
    .describe(
      'Filter workspaces by matching values in the `extra` JSON payload.\n\nFormat: `\"field:value\"` for single condition or `\"f1:v1,f2:v2\"` for multiple (AND).'
    )
    .or(zod.null())
    .optional()
})

export const listTeamsWorkspacesResponseDataItemWorkspaceIdRegExp =
  /^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/

export const listTeamsWorkspacesResponse = zod.object({
  success: zod.literal(true),
  data: zod.array(
    zod.object({
      workspace_id: zod
        .string()
        .uuid()
        .regex(listTeamsWorkspacesResponseDataItemWorkspaceIdRegExp)
        .describe("The unique identifier (UUID) of the teams workspace"),
      name: zod.string(),
      domain: zod.string(),
      state: zod
        .enum(["active", "invalid"])
        .describe(
          "The current state of the teams workspace.\n\n- `active`: bots can sign in with accounts under this tenant\n- `invalid`: flipped to cascade-disable the whole tenant. Manual re-enable required."
        ),
      failure_data: zod.any().optional(),
      last_error_message: zod.string().or(zod.null()),
      last_error_at: zod.string().or(zod.null()),
      extra: zod
        .record(zod.string(), zod.any())
        .describe(
          "An optional free-form JSON object you can attach to the workspace.\n\nStored as-is and never interpreted by the API. Filterable on the list endpoint via the `extra` query parameter using the `key:value` syntax."
        )
        .or(zod.null()),
      created_at: zod.string(),
      updated_at: zod.string()
    })
  )
})

/**
 * @summary Get a teams workspace
 */
export const getTeamsWorkspacePathWorkspaceIdRegExp =
  /^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/

export const getTeamsWorkspaceParams = zod.object({
  workspace_id: zod
    .string()
    .uuid()
    .regex(getTeamsWorkspacePathWorkspaceIdRegExp)
    .describe("The unique identifier (UUID) of the teams workspace")
})

export const getTeamsWorkspaceResponseDataWorkspaceIdRegExp =
  /^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/

export const getTeamsWorkspaceResponse = zod.object({
  success: zod.literal(true),
  data: zod.object({
    workspace_id: zod
      .string()
      .uuid()
      .regex(getTeamsWorkspaceResponseDataWorkspaceIdRegExp)
      .describe("The unique identifier (UUID) of the teams workspace"),
    name: zod.string(),
    domain: zod.string(),
    state: zod
      .enum(["active", "invalid"])
      .describe(
        "The current state of the teams workspace.\n\n- `active`: bots can sign in with accounts under this tenant\n- `invalid`: flipped to cascade-disable the whole tenant. Manual re-enable required."
      ),
    failure_data: zod.any().optional(),
    last_error_message: zod.string().or(zod.null()),
    last_error_at: zod.string().or(zod.null()),
    extra: zod
      .record(zod.string(), zod.any())
      .describe(
        "An optional free-form JSON object you can attach to the workspace.\n\nStored as-is and never interpreted by the API. Filterable on the list endpoint via the `extra` query parameter using the `key:value` syntax."
      )
      .or(zod.null()),
    created_at: zod.string(),
    updated_at: zod.string()
  })
})

/**
 * @summary Update a teams workspace
 */
export const updateTeamsWorkspacePathWorkspaceIdRegExp =
  /^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/

export const updateTeamsWorkspaceParams = zod.object({
  workspace_id: zod
    .string()
    .uuid()
    .regex(updateTeamsWorkspacePathWorkspaceIdRegExp)
    .describe("The unique identifier (UUID) of the teams workspace")
})

export const updateTeamsWorkspaceBodyNameMax = 100

export const updateTeamsWorkspaceBody = zod.object({
  name: zod
    .string()
    .min(1)
    .max(updateTeamsWorkspaceBodyNameMax)
    .optional()
    .describe(
      "User-friendly label for the workspace. Not unique — names can repeat within a team."
    ),
  state: zod
    .enum(["active"])
    .optional()
    .describe(
      "Only 'active' is allowed via PATCH (system-only sets 'invalid'). Use this to re-enable a workspace after fixing the underlying failure."
    ),
  extra: zod
    .record(zod.string(), zod.any())
    .describe(
      "An optional free-form JSON object you can attach to the workspace.\n\nStored as-is and never interpreted by the API. Filterable on the list endpoint via the `extra` query parameter using the `key:value` syntax."
    )
    .or(zod.null())
    .optional()
})

export const updateTeamsWorkspaceResponseDataWorkspaceIdRegExp =
  /^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/

export const updateTeamsWorkspaceResponse = zod.object({
  success: zod.literal(true),
  data: zod.object({
    workspace_id: zod
      .string()
      .uuid()
      .regex(updateTeamsWorkspaceResponseDataWorkspaceIdRegExp)
      .describe("The unique identifier (UUID) of the teams workspace"),
    name: zod.string(),
    domain: zod.string(),
    state: zod
      .enum(["active", "invalid"])
      .describe(
        "The current state of the teams workspace.\n\n- `active`: bots can sign in with accounts under this tenant\n- `invalid`: flipped to cascade-disable the whole tenant. Manual re-enable required."
      ),
    failure_data: zod.any().optional(),
    last_error_message: zod.string().or(zod.null()),
    last_error_at: zod.string().or(zod.null()),
    extra: zod
      .record(zod.string(), zod.any())
      .describe(
        "An optional free-form JSON object you can attach to the workspace.\n\nStored as-is and never interpreted by the API. Filterable on the list endpoint via the `extra` query parameter using the `key:value` syntax."
      )
      .or(zod.null()),
    created_at: zod.string(),
    updated_at: zod.string()
  })
})

/**
 * @summary Delete a teams workspace (cascades to its logins)
 */
export const deleteTeamsWorkspacePathWorkspaceIdRegExp =
  /^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/

export const deleteTeamsWorkspaceParams = zod.object({
  workspace_id: zod
    .string()
    .uuid()
    .regex(deleteTeamsWorkspacePathWorkspaceIdRegExp)
    .describe("The unique identifier (UUID) of the teams workspace")
})

export const deleteTeamsWorkspaceResponse = zod.object({
  success: zod.literal(true),
  data: zod.object({
    message: zod.string()
  })
})
