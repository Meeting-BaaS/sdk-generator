/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * OpenAI Audio & Realtime API
 * OpenAI Audio API - Transcription, Translation, Speech, and Realtime streaming endpoints. Filtered from the official OpenAI API spec (Stainless-hosted).
 * OpenAPI spec version: 2.3.0
 */
import type { MCPTool } from './mCPTool.js';
import type { Prompt } from './prompt.js';
import type { RealtimeFunctionTool } from './realtimeFunctionTool.js';
import type { RealtimeReasoning } from './realtimeReasoning.js';
import type { RealtimeSessionCreateResponseGAAudio } from './realtimeSessionCreateResponseGAAudio.js';
import type { RealtimeSessionCreateResponseGAIncludeItem } from './realtimeSessionCreateResponseGAIncludeItem.js';
import type { RealtimeSessionCreateResponseGAObject } from './realtimeSessionCreateResponseGAObject.js';
import type { RealtimeSessionCreateResponseGAOutputModalitiesItem } from './realtimeSessionCreateResponseGAOutputModalitiesItem.js';
import type { RealtimeSessionCreateResponseGATracing } from './realtimeSessionCreateResponseGATracing.js';
import type { RealtimeSessionCreateResponseGAType } from './realtimeSessionCreateResponseGAType.js';
import type { RealtimeTruncation } from './realtimeTruncation.js';
import type { ToolChoiceFunction } from './toolChoiceFunction.js';
import type { ToolChoiceMCP } from './toolChoiceMCP.js';
import type { ToolChoiceOptions } from './toolChoiceOptions.js';

/**
 * A Realtime session configuration object.
 */
export interface RealtimeSessionCreateResponseGA {
  /** The type of session to create. Always `realtime` for the Realtime API. */
  type: RealtimeSessionCreateResponseGAType;
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`. */
  id: string;
  /** The object type. Always `realtime.session`. */
  object: RealtimeSessionCreateResponseGAObject;
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at?: number;
  /**
     * The set of modalities the model can respond with. It defaults to `["audio"]`, indicating
     * that the model will respond with audio plus a transcript. `["text"]` can be used to make
     * the model respond with text only. It is not possible to request both `text` and `audio` at the same time.
     */
  output_modalities?: RealtimeSessionCreateResponseGAOutputModalitiesItem[];
  /** The Realtime model used for this session. */
  model?: string | 'gpt-realtime' | 'gpt-realtime-1.5' | 'gpt-realtime-2' | 'gpt-realtime-2.1' | 'gpt-realtime-2.1-mini' | 'gpt-realtime-2025-08-28' | 'gpt-4o-realtime-preview' | 'gpt-4o-realtime-preview-2024-10-01' | 'gpt-4o-realtime-preview-2024-12-17' | 'gpt-4o-realtime-preview-2025-06-03' | 'gpt-4o-mini-realtime-preview' | 'gpt-4o-mini-realtime-preview-2024-12-17' | 'gpt-realtime-mini' | 'gpt-realtime-mini-2025-10-06' | 'gpt-realtime-mini-2025-12-15' | 'gpt-audio-1.5' | 'gpt-audio-mini' | 'gpt-audio-mini-2025-10-06' | 'gpt-audio-mini-2025-12-15';
  /**
     * The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
     *
     * Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
     */
  instructions?: string;
  /** Configuration for input and output audio. */
  audio?: RealtimeSessionCreateResponseGAAudio;
  /**
     * Additional fields to include in server outputs.
     *
     * `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
     */
  include?: RealtimeSessionCreateResponseGAIncludeItem[];
  tracing?: RealtimeSessionCreateResponseGATracing;
  /** Tools available to the model. */
  tools?: (RealtimeFunctionTool | MCPTool)[];
  /**
     * How the model chooses tools. Provide one of the string modes or force a specific
     * function/MCP tool.
     */
  tool_choice?: ToolChoiceOptions | ToolChoiceFunction | ToolChoiceMCP;
  reasoning?: RealtimeReasoning;
  /**
     * Maximum number of output tokens for a single assistant response,
     * inclusive of tool calls. Provide an integer between 1 and 4096 to
     * limit output tokens, or `inf` for the maximum available tokens for a
     * given model. Defaults to `inf`.
     */
  max_output_tokens?: number | 'inf';
  truncation?: RealtimeTruncation;
  prompt?: Prompt | null;
}
