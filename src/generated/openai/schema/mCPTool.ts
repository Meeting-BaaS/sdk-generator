/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * OpenAI Audio & Realtime API
 * OpenAI Audio API - Transcription, Translation, Speech, and Realtime streaming endpoints. Filtered from the official OpenAI API spec (Stainless-hosted).
 * OpenAPI spec version: 2.3.0
 */
import type { CallableToolAllowedCaller } from './callableToolAllowedCaller.js';
import type { MCPToolConnectorId } from './mCPToolConnectorId.js';
import type { MCPToolFilter } from './mCPToolFilter.js';
import type { MCPToolHeaders } from './mCPToolHeaders.js';
import type { MCPToolRequireApproval } from './mCPToolRequireApproval.js';
import type { MCPToolType } from './mCPToolType.js';

/**
 * Give the model access to additional tools via remote Model Context Protocol
 * (MCP) servers. [Learn more about MCP](/docs/guides/tools-remote-mcp).
 */
export interface MCPTool {
  /** The type of the MCP tool. Always `mcp`. */
  type: MCPToolType;
  /** A label for this MCP server, used to identify it in tool calls. */
  server_label: string;
  /**
     * The URL for the MCP server. One of `server_url`, `connector_id`, or
     * `tunnel_id` must be provided.
     */
  server_url?: string;
  /**
     * Identifier for service connectors, like those available in ChatGPT. One of
     * `server_url`, `connector_id`, or `tunnel_id` must be provided. Learn more
     * about service connectors [here](/docs/guides/tools-remote-mcp#connectors).
     *
     * Currently supported `connector_id` values are:
     *
     * - Dropbox: `connector_dropbox`
     * - Gmail: `connector_gmail`
     * - Google Calendar: `connector_googlecalendar`
     * - Google Drive: `connector_googledrive`
     * - Microsoft Teams: `connector_microsoftteams`
     * - Outlook Calendar: `connector_outlookcalendar`
     * - Outlook Email: `connector_outlookemail`
     * - SharePoint: `connector_sharepoint`
     */
  connector_id?: MCPToolConnectorId;
  /**
     * The Secure MCP Tunnel ID to use instead of a direct server URL. One of
     * `server_url`, `connector_id`, or `tunnel_id` must be provided.
     * @pattern ^tunnel_[a-z0-9]{32}$
     */
  tunnel_id?: string;
  /**
     * An OAuth access token that can be used with a remote MCP server, either
     * with a custom MCP server URL or a service connector. Your application
     * must handle the OAuth authorization flow and provide the token here.
     */
  authorization?: string;
  /** Optional description of the MCP server, used to provide more context. */
  server_description?: string;
  headers?: MCPToolHeaders;
  allowed_tools?: string[] | MCPToolFilter | null;
  allowed_callers?: CallableToolAllowedCaller[] | null;
  require_approval?: MCPToolRequireApproval;
  /** Whether this MCP tool is deferred and discovered via tool search. */
  defer_loading?: boolean;
}
