import { defineConfig } from "vitest/config"

export default defineConfig({
  test: {
    globals: true,
    environment: "node",
    setupFiles: ["./test/setup.ts"],
    coverage: {
      provider: "v8",
      reporter: ["text", "json", "html"],
      exclude: [
        "node_modules/",
        "dist/",
        "test/",
        "**/*.d.ts",
        "**/*.config.*",
        // This is just an entry point for the SDK
        "src/index.ts",
        // This is generated by orval.
        // The generated files are consumed in the src/baas directory which we are testing
        // So we need to exclude them from coverage
        "src/generated/**",
        // These are scripts used to generate and process the OpenAPI schema.
        "scripts/**",
        // These are examples of how to use the SDK.
        "examples/**"
      ]
    }
  }
})
